feat(desktop): Publish portable Electron package

同一上游提交、同一 Release 里新增 dsh-desktop-win64-*.zip：Electron 外壳 + 内置
dsh 运行时。构建期关掉上游自带更新（产物无 feed、manifest 无策略），并保留便携
习惯（DSH_HOME 与 Electron 目录落包内、按 VERSION 迁移会话）。

- portable/build-desktop.mjs：七步流水线（补丁→构建→P1→迁移器→打包→组装→验收）
- portable/patch-desktop-*.mjs：更新禁用、便携引导、安装器/冒烟开关、内部重建开关
- portable/dsh-updater.cs：清单驱动（读 update.json），缺清单时逐字节回落原行为
- portable/desktop-seed：桌面 profile 的习惯种子（配置 + 自定义预设）
- workflow：新增 desktop job；release 一并挂资产；workflow_dispatch 增加 channel 输入
